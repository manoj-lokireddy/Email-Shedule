export type User = { id: string; name: string; email: string; avatar?: string }; export type Sender = { id: string; email: string; name: string }; export type EmailJob = { id: string; recipientEmail: string; subject: string; body: string; scheduledAt: string; sentAt?: string; status: string; attempts: number; errorMessage?: string }; export type Slack = { teamName: string; connected: boolean } | null;
